import { StyleSheet } from "react-native";

const layout = StyleSheet.create({
  container: {
    width: "100%",
    height: "100%",
  },
  headerRight: {
    marginRight: 10,
  },
});

export default layout;
